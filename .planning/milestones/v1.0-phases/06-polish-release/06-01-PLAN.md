---
phase: 06-polish-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gsd-ml/workflows/ml-run.md
  - gsd-ml/workflows/ml-resume.md
  - gsd-ml/workflows/ml-status.md
  - gsd-ml/workflows/ml-clean.md
  - gsd-ml/workflows/ml-diagnose.md
  - package.json
autonomous: true
requirements:
  - POLISH-01
  - POLISH-03
must_haves:
  truths:
    - "All workflow Python bridge calls use python3 (not python)"
    - "Each workflow has a pre-flight check for gsd_ml availability"
    - "Workflows handle missing .ml/ state, corrupt checkpoint, missing dataset gracefully"
    - "package.json has homepage and bugs fields"
    - "All Python bridge calls in workflows match actual gsd_ml API signatures"
    - "Python test suite passes (188+ tests)"
  artifacts:
    - path: "gsd-ml/workflows/ml-run.md"
      provides: "Main experiment workflow with error handling"
      contains: "python3 -c"
    - path: "gsd-ml/workflows/ml-resume.md"
      provides: "Resume workflow with pre-flight checks"
      contains: "python3 -c"
    - path: "package.json"
      provides: "npm metadata with homepage and bugs fields"
      contains: "homepage"
  key_links:
    - from: "gsd-ml/workflows/*.md"
      to: "python/src/gsd_ml/*.py"
      via: "python3 -c bridge calls"
      pattern: "from gsd_ml"
---

<objective>
Harden workflow error handling, standardize python3 calls, validate Python bridge signatures, and polish package.json metadata.

Purpose: Make workflows robust against common failure modes (missing deps, corrupt state, missing files) and ensure package.json is publish-ready.
Output: 5 updated workflow files, updated package.json, verified bridge call correctness.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-release/06-CONTEXT.md
@.planning/phases/06-polish-release/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate Python bridge calls and standardize python3</name>
  <files>gsd-ml/workflows/ml-run.md, gsd-ml/workflows/ml-resume.md, gsd-ml/workflows/ml-status.md, gsd-ml/workflows/ml-clean.md, gsd-ml/workflows/ml-diagnose.md</files>
  <action>
First, audit all Python bridge calls across the 5 workflow files. For each `python -c "from gsd_ml.X import Y"` or `python3 -c "from gsd_ml.X import Y"` call:
1. Verify the import path exists in python/src/gsd_ml/
2. Verify the function/class name exists with the expected signature
3. Verify the call arguments match the actual function signature

Use grep to find all bridge calls, then cross-reference against the actual Python source.

Then apply these changes to ALL 5 workflow files:
1. Replace every `python -c` with `python3 -c` (standardize to python3 everywhere)
2. Add a "Pre-flight Checks" section near the top of each workflow (after any intro text, before the first substantive step) with:
   - `python3 -c "import gsd_ml; print('OK')"` check -- if fails, STOP with actionable error: "gsd_ml Python package not installed. Install with: pip install gsd-ml (from PyPI) or pip install ./python (from repo)"
   - `git status --porcelain` check (for ml-run.md and ml-resume.md only) -- if not a git repo, STOP with: "This directory must be a git repository. Run git init first."
3. Add workflow-specific defensive checks per the RESEARCH.md table:
   - ml-run.md: verify dataset file/directory exists before profiling, verify git branch can be created
   - ml-resume.md: verify .ml/ directory exists, verify checkpoint.json exists and loads without error, verify the git branch from checkpoint still exists
   - ml-status.md: (pre-flight only -- already minimal)
   - ml-clean.md: verify .ml/ directory exists before attempting cleanup
   - ml-diagnose.md: verify .ml/ directory and checkpoint exist

Keep error handling minimal and actionable per user decision: "Don't add try/catch to every Python bridge call -- only where failure is likely (missing deps, corrupt state)". The primary user is Claude Code following instructions, not humans typing commands.

Fix any bridge call mismatches found in the audit.
  </action>
  <verify>
    <automated>cd /home/tlupo/gsd-ml && grep -rn 'python -c' gsd-ml/workflows/ | grep -v 'python3 -c' | wc -l | grep -q '^0$' && echo "PASS: no bare python -c calls" || echo "FAIL: found bare python -c calls"</automated>
  </verify>
  <done>All workflow files use python3 consistently, each has pre-flight checks, bridge calls verified against actual Python API, defensive checks added at likely failure points</done>
</task>

<task type="auto">
  <name>Task 2: Polish package.json and run test suite</name>
  <files>package.json</files>
  <action>
Update package.json to add missing fields for npm publish readiness:
1. Add `"homepage": "https://github.com/robertlupo1997/gsd-ml#readme"`
2. Add `"bugs": { "url": "https://github.com/robertlupo1997/gsd-ml/issues" }`
3. Verify version is "0.1.0" (per user decision)
4. Verify `files` array includes ["bin", "commands", "gsd-ml"] (already correct per research)
5. Verify `bin.gsd-ml` points to "bin/install.js"

Then run the full Python test suite to confirm all tests still pass:
```bash
source .venv/bin/activate && cd python && python -m pytest -q
```

Then verify npm pack still works:
```bash
cd /home/tlupo/gsd-ml && npm pack --dry-run
```

Verify the packed tarball includes the expected files (bin/, commands/, gsd-ml/, package.json, README.md will be added in Plan 02).
  </action>
  <verify>
    <automated>cd /home/tlupo/gsd-ml && source .venv/bin/activate && cd python && python -m pytest -q && cd /home/tlupo/gsd-ml && npm pack --dry-run 2>&1 | tail -5</automated>
  </verify>
  <done>package.json has homepage and bugs fields, version is 0.1.0, all Python tests pass, npm pack succeeds with correct file list</done>
</task>

</tasks>

<verification>
1. `grep -rn 'python -c' gsd-ml/workflows/ | grep -v 'python3 -c'` returns empty (no bare python calls)
2. `grep -c 'Pre-flight' gsd-ml/workflows/*.md` shows count for each workflow file
3. `source .venv/bin/activate && cd python && python -m pytest -q` passes all tests
4. `npm pack --dry-run` succeeds
5. `node -e "const p=require('./package.json'); console.log(p.homepage, p.bugs)"` shows expected values
</verification>

<success_criteria>
- All 5 workflow files standardized to python3
- Pre-flight checks in every workflow
- Bridge calls verified against actual Python source (no signature mismatches)
- package.json publish-ready with homepage, bugs, correct version
- Full Python test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-release/06-01-SUMMARY.md`
</output>
