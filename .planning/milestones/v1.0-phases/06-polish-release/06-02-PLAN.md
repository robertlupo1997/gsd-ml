---
phase: 06-polish-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: false
requirements:
  - POLISH-02
  - POLISH-04
must_haves:
  truths:
    - "README.md exists at repo root with all required sections"
    - "README documents both npm and pip install steps"
    - "README includes Quick Start with tabular example"
    - "README includes all 3 domains (tabular, DL, FT) with commands"
    - "README includes skills reference table (5 skills)"
    - "README includes From mlforge migration section"
    - "npm publish --dry-run succeeds"
  artifacts:
    - path: "README.md"
      provides: "Project documentation"
      min_lines: 80
      contains: "npm install -g gsd-ml"
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "install instructions match package name"
      pattern: "npm install.*gsd-ml"
---

<objective>
Create README.md and verify npm publish readiness.

Purpose: Document installation, usage, and architecture for users. Verify the package can be published to npm.
Output: README.md at repo root, successful npm publish --dry-run.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-release/06-CONTEXT.md
@.planning/phases/06-polish-release/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md and verify npm publish</name>
  <files>README.md</files>
  <action>
Create README.md at repo root. Per user decisions, include these sections (keep concise -- power-user tool, not beginner tutorial):

1. **Title + badges**: gsd-ml with npm version badge and MIT license badge (shields.io)
2. **What is this?**: 2-3 sentences -- Claude Code native ML research tool, runs structured experiments autonomously, no subprocess spawning
3. **Install**: Two steps: `npm install -g gsd-ml && gsd-ml` (copies skills/workflows to ~/.claude/), then `pip install gsd-ml` or `pip install ./python` (Python utilities)
4. **Quick Start**: Show `/gsd:ml data.csv target_column` with brief explanation
5. **Domains table**: Tabular (sklearn, XGBoost, LightGBM), DL Image (timm), DL Text (transformers), Fine-Tuning (peft, trl) -- with example commands for each
6. **Skills table**: /gsd:ml, /gsd:ml-status, /gsd:ml-resume, /gsd:ml-clean, /gsd:ml-diagnose with one-line descriptions
7. **How It Works**: Brief architecture -- skills trigger workflows, workflows instruct Claude Code to edit train.py/run/parse metrics/keep or revert via git. Mention .ml/ state directory, config.json, checkpoint.json
8. **From mlforge**: For existing mlforge users -- gsd-ml replaces the subprocess-spawning CLI with native Claude Code execution. Same Python utilities (gsd_ml replaces mlforge imports), same experiment structure, but Claude Code IS the researcher instead of being spawned as subprocess
9. **Requirements**: Python 3.11+ (per pyproject.toml), Node.js 16.7+, Claude Code

Use the README structure from RESEARCH.md as a guide. Use real command examples from the codebase.

After creating README.md, verify npm publish readiness:
```bash
npm publish --dry-run
```

This should succeed and show the correct package name, version 0.1.0, and file list including README.md.

Also run:
```bash
npm pack --dry-run
```

Verify README.md is included in the packed files (npm auto-includes root README).
  </action>
  <verify>
    <automated>cd /home/tlupo/gsd-ml && test -f README.md && npm publish --dry-run 2>&1 | grep -q "gsd-ml" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README.md exists with all required sections, npm publish --dry-run succeeds, package ready for actual publish</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User reviews README and approves publish</name>
  <files>README.md</files>
  <action>
Present README.md and npm publish --dry-run output for user review. This is the final gate before the package can be published to npm.
  </action>
  <verify>User confirms README content and publish readiness</verify>
  <done>User approves README and npm publish readiness</done>
  <what-built>README.md documentation and npm publish readiness. The package is ready for npm publish.</what-built>
  <how-to-verify>
    1. Read README.md -- verify it covers: install, quick start, domains, skills, architecture, mlforge migration, requirements
    2. Review npm publish --dry-run output -- verify package name, version 0.1.0, file list looks correct
    3. If you want to publish now: run npm login (if needed) then npm publish
    4. If you want changes to README: describe what to adjust
  </how-to-verify>
  <resume-signal>Type "approved" to complete the phase, or describe changes needed</resume-signal>
</task>

</tasks>

<verification>
1. `test -f README.md` -- file exists
2. `wc -l README.md` -- at least 80 lines
3. `grep -c 'npm install' README.md` -- install instructions present
4. `grep -c 'gsd:ml' README.md` -- skill references present
5. `grep -c 'mlforge' README.md` -- migration section present
6. `npm publish --dry-run` -- succeeds without errors
</verification>

<success_criteria>
- README.md at repo root with all sections from user decisions
- Concise, power-user focused (not a beginner tutorial)
- npm publish --dry-run succeeds
- User approves README content and publish readiness
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-release/06-02-SUMMARY.md`
</output>
