---
phase: 04-dl-fine-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gsd-ml/templates/train-dl-image.py
  - gsd-ml/templates/train-dl-text.py
  - gsd-ml/templates/train-ft.py
  - gsd-ml/references/metric-map.md
  - python/src/gsd_ml/baselines/deeplearning.py
  - python/src/gsd_ml/baselines/finetuning.py
  - python/tests/test_baselines.py
  - python/tests/test_templates.py
autonomous: true
requirements: [DL-01, DL-02, DL-03, DL-04, FT-01, FT-02, FT-03, FT-04]

must_haves:
  truths:
    - "DL image classification template exists with __PLACEHOLDER__ constants and timm model creation"
    - "DL text classification template exists with __PLACEHOLDER__ constants and transformers model creation"
    - "FT template exists with QLoRA config, LoRA adapters, SFTTrainer, and __PLACEHOLDER__ constants"
    - "DL baselines have passes_baseline_gate() that works for both maximize and minimize directions"
    - "FT baselines have passes_baseline_gate() that works for minimize direction (perplexity, loss)"
    - "Metric map includes DL metrics (accuracy, f1_weighted, loss) and FT metrics (perplexity, rouge1, rougeL, loss)"
  artifacts:
    - path: "gsd-ml/templates/train-dl-image.py"
      provides: "Image classification training template"
      contains: "__DATA_PATH__"
    - path: "gsd-ml/templates/train-dl-text.py"
      provides: "Text classification training template"
      contains: "__DATA_PATH__"
    - path: "gsd-ml/templates/train-ft.py"
      provides: "Fine-tuning training template"
      contains: "__MODEL_NAME__"
    - path: "gsd-ml/references/metric-map.md"
      provides: "Metric reference for all domains"
      contains: "perplexity"
    - path: "python/src/gsd_ml/baselines/deeplearning.py"
      provides: "DL baseline computation + gate"
      exports: ["compute_baselines", "passes_baseline_gate"]
    - path: "python/src/gsd_ml/baselines/finetuning.py"
      provides: "FT baseline computation + gate"
      exports: ["compute_baselines", "passes_baseline_gate"]
  key_links:
    - from: "gsd-ml/templates/train-dl-image.py"
      to: "prepare.load_image_data"
      via: "import from prepare"
      pattern: "from prepare import.*load_image_data"
    - from: "gsd-ml/templates/train-ft.py"
      to: "prepare.format_dataset"
      via: "import from prepare"
      pattern: "from prepare import.*format_dataset"
---

<objective>
Create all DL and FT building blocks: three static training templates (de-Jinja2'd from mlforge), baseline gate functions, metric map extensions, and tests.

Purpose: These are the artifacts the workflow (Plan 02) will reference when routing DL/FT experiments.
Output: 3 template files, 2 updated Python modules, updated metric map, 2 test files
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dl-fine-tuning/04-RESEARCH.md

Source templates to port (de-Jinja2):
@/home/tlupo/AutoML/src/mlforge/templates/dl_train.py.j2
@/home/tlupo/AutoML/src/mlforge/templates/ft_train.py.j2

Existing pattern to follow:
@gsd-ml/templates/train-tabular.py
@gsd-ml/references/metric-map.md

Existing baseline modules to extend:
@python/src/gsd_ml/baselines/deeplearning.py
@python/src/gsd_ml/baselines/finetuning.py
@python/src/gsd_ml/baselines/tabular.py (reference for passes_baseline_gate pattern)

Existing tests to extend:
@python/tests/test_baselines.py
</context>

<interfaces>
<!-- From baselines/tabular.py -- the passes_baseline_gate pattern to replicate -->
```python
def passes_baseline_gate(
    metric_value: float,
    baselines: dict[str, dict[str, float]],
    direction: str,
) -> bool:
    """Check if a metric value beats ALL baselines."""
    for baseline_info in baselines.values():
        baseline_score = baseline_info["score"]
        if direction == "maximize" and metric_value <= baseline_score:
            return False
        if direction == "minimize" and metric_value >= baseline_score:
            return False
    return True
```

<!-- From train-tabular.py -- the __PLACEHOLDER__ pattern -->
Constants use `__UPPER_SNAKE__` format, replaced by workflow during scaffold.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DL/FT templates, baseline gates, and metric map</name>
  <files>
    gsd-ml/templates/train-dl-image.py,
    gsd-ml/templates/train-dl-text.py,
    gsd-ml/templates/train-ft.py,
    gsd-ml/references/metric-map.md,
    python/src/gsd_ml/baselines/deeplearning.py,
    python/src/gsd_ml/baselines/finetuning.py
  </files>
  <behavior>
    - DL image template has __DATA_PATH__, __IMG_SIZE__, __BATCH_SIZE__, __MODEL_NAME__, __METRIC__, __TIME_BUDGET_SEC__ placeholders
    - DL image template imports timm and prepare.load_image_data
    - DL image template uses torch.amp.autocast + GradScaler for mixed precision (DL-03)
    - DL image template saves predictions.csv and best_model.pt
    - DL image template prints JSON result on stdout
    - DL text template has __DATA_PATH__, __MODEL_NAME__, __BATCH_SIZE__, __METRIC__, __TIME_BUDGET_SEC__ placeholders
    - DL text template imports AutoModelForSequenceClassification and prepare.load_text_data
    - DL text template uses torch.amp.autocast + GradScaler for mixed precision (DL-03)
    - FT template has __MODEL_NAME__, __LORA_R__, __LORA_ALPHA__, __METRIC__, __MAX_LENGTH__, __BATCH_SIZE__, __LEARNING_RATE__, __NUM_EPOCHS__, __DATA_PATH__ placeholders
    - FT template uses BitsAndBytesConfig, LoraConfig, get_peft_model, SFTTrainer
    - FT template uses processing_class=tokenizer (not deprecated tokenizer= param) per research
    - FT template has evaluate_perplexity() and evaluate_rouge() functions
    - FT template saves adapter to best_adapter/ directory
    - passes_baseline_gate(0.8, {"random": {"score": 0.5}}, "maximize") returns True for DL
    - passes_baseline_gate(0.3, {"random": {"score": 0.5}}, "maximize") returns False for DL
    - passes_baseline_gate(2.0, {"random_guess": {"score": 10.0}}, "minimize") returns True for FT
    - passes_baseline_gate(15.0, {"random_guess": {"score": 10.0}}, "minimize") returns False for FT
    - Metric map has DL section with accuracy, f1_weighted, loss
    - Metric map has FT section with perplexity, rouge1, rougeL, loss
  </behavior>
  <action>
    1. Create `gsd-ml/templates/train-dl-image.py`: De-Jinja2 the image_classification path from `/home/tlupo/AutoML/src/mlforge/templates/dl_train.py.j2`. Replace all `{{ var }}` with `__UPPER_SNAKE__` constants. Remove all Jinja2 conditionals -- keep ONLY the image_classification code path. Keep the full training loop with mixed precision (autocast + GradScaler), early stopping, predictions.csv saving, and JSON output. Import from `prepare` (not gsd_ml). Change docstring to say "Generated by gsd-ml scaffold phase."

    2. Create `gsd-ml/templates/train-dl-text.py`: Same de-Jinja2 process but keep ONLY the text_classification code path. Uses `AutoModelForSequenceClassification` from transformers. Text-specific batch handling (input_ids, attention_mask, labels dict). Import `load_text_data` from prepare.

    3. Create `gsd-ml/templates/train-ft.py`: De-Jinja2 from `/home/tlupo/AutoML/src/mlforge/templates/ft_train.py.j2`. Replace `{{ var }}` with `__UPPER_SNAKE__` constants. Change `data_path="data.json"` to `data_path="__DATA_PATH__"`. Change `SFTTrainer(tokenizer=tokenizer)` to `SFTTrainer(processing_class=tokenizer)` per research recommendation. Keep evaluate_perplexity(), evaluate_rouge(), QLoRA setup, LoRA setup, adapter saving. Change docstring to say "Generated by gsd-ml scaffold phase."

    4. Add `passes_baseline_gate()` to `python/src/gsd_ml/baselines/deeplearning.py`: Copy the exact same implementation from tabular.py. The function signature and logic are identical -- it checks if metric_value strictly beats all baseline scores in the given direction.

    5. Add `passes_baseline_gate()` to `python/src/gsd_ml/baselines/finetuning.py`: Same implementation. FT metrics (perplexity, loss) use direction="minimize", ROUGE metrics use direction="maximize".

    6. Update `gsd-ml/references/metric-map.md`: Add two new sections after the existing tabular section:
       - "## Deep Learning Metrics" with accuracy (maximize), f1_weighted (maximize), loss (minimize)
       - "## Fine-Tuning Metrics" with perplexity (minimize), loss (minimize), rouge1 (maximize), rougeL (maximize)
       - Add DL metric selection logic: image_classification defaults to accuracy, text_classification defaults to f1_weighted
       - Add FT metric defaults: perplexity (default), loss, rouge1, rougeL
  </action>
  <verify>
    <automated>cd /home/tlupo/gsd-ml && python -m pytest python/tests/test_baselines.py python/tests/test_templates.py -x -q</automated>
  </verify>
  <done>
    - 3 template files exist with correct placeholders and no Jinja2 syntax
    - passes_baseline_gate works in both DL and FT baseline modules
    - Metric map covers all 3 domains (tabular, DL, FT)
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create test files for templates and DL/FT baseline gates</name>
  <files>
    python/tests/test_templates.py,
    python/tests/test_baselines.py
  </files>
  <action>
    1. Create `python/tests/test_templates.py` with tests that verify template FILE CONTENT (not execution -- these templates need GPU/torch to run):
       - `test_dl_image_template_exists`: File exists at gsd-ml/templates/train-dl-image.py
       - `test_dl_image_template_placeholders`: Contains __DATA_PATH__, __IMG_SIZE__, __BATCH_SIZE__, __MODEL_NAME__, __METRIC__, __TIME_BUDGET_SEC__
       - `test_dl_image_template_imports`: Contains "import timm" and "from prepare import.*load_image_data"
       - `test_dl_image_template_no_jinja`: Does NOT contain "{{" or "{%"
       - `test_dl_image_template_mixed_precision`: Contains "torch.amp.autocast" and "GradScaler"
       - `test_dl_text_template_exists`: File exists at gsd-ml/templates/train-dl-text.py
       - `test_dl_text_template_placeholders`: Contains __DATA_PATH__, __MODEL_NAME__, __BATCH_SIZE__, __METRIC__, __TIME_BUDGET_SEC__
       - `test_dl_text_template_imports`: Contains "AutoModelForSequenceClassification" and "from prepare import.*load_text_data"
       - `test_dl_text_template_no_jinja`: No Jinja2 syntax
       - `test_ft_template_exists`: File exists at gsd-ml/templates/train-ft.py
       - `test_ft_template_placeholders`: Contains __MODEL_NAME__, __LORA_R__, __LORA_ALPHA__, __METRIC__, __MAX_LENGTH__, __BATCH_SIZE__, __LEARNING_RATE__, __NUM_EPOCHS__, __DATA_PATH__
       - `test_ft_template_qlora`: Contains "BitsAndBytesConfig" and "get_peft_model"
       - `test_ft_template_sfttrainer`: Contains "SFTTrainer" and "processing_class"
       - `test_ft_template_no_jinja`: No Jinja2 syntax
       - `test_ft_metrics_functions`: Contains "evaluate_perplexity" and "evaluate_rouge"
       Use `Path(__file__).parent.parent.parent / "gsd-ml" / "templates"` to locate template files.

    2. Add to `python/tests/test_baselines.py` two new test classes:
       - `TestDLBaselines`: test compute_baselines for classification metrics and loss metric, test passes_baseline_gate maximize and minimize
       - `TestFTBaselines`: test compute_baselines for loss and perplexity metrics, test passes_baseline_gate minimize direction
       Follow the exact same pattern as existing TestComputeBaselines and TestPassesBaselineGate classes.

    NOTE: Task 2 should be executed BEFORE Task 1 (TDD -- write tests first, then implement). The executor should write tests, verify they fail, then implement Task 1, then verify they pass.
  </action>
  <verify>
    <automated>cd /home/tlupo/gsd-ml && python -m pytest python/tests/test_baselines.py python/tests/test_templates.py -x -q</automated>
  </verify>
  <done>
    - test_templates.py has 15+ tests covering all 3 templates
    - test_baselines.py has TestDLBaselines and TestFTBaselines classes
    - All tests pass after Task 1 implementation
  </done>
</task>

</tasks>

<verification>
1. All 3 templates exist and contain no Jinja2 syntax: `grep -r '{{' gsd-ml/templates/train-dl-*.py gsd-ml/templates/train-ft.py` returns nothing
2. Both DL and FT baseline modules export passes_baseline_gate: `python -c "from gsd_ml.baselines.deeplearning import passes_baseline_gate; from gsd_ml.baselines.finetuning import passes_baseline_gate; print('OK')"`
3. Full test suite passes: `cd /home/tlupo/gsd-ml && python -m pytest python/tests/ -x -q`
</verification>

<success_criteria>
- 3 static training templates created with __PLACEHOLDER__ constants
- passes_baseline_gate added to both DL and FT baseline modules
- Metric map covers tabular + DL + FT domains
- All existing + new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-dl-fine-tuning/04-01-SUMMARY.md`
</output>
