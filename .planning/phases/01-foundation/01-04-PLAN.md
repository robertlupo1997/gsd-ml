---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - python/tests/conftest.py
  - python/tests/test_imports.py
  - python/tests/test_profiler.py
  - python/tests/test_state.py
  - python/tests/test_checkpoint.py
  - python/tests/test_guardrails.py
  - python/tests/test_diagnostics.py
  - python/tests/test_results.py
  - python/tests/test_journal.py
  - python/tests/test_stagnation.py
  - python/tests/test_drafts.py
  - python/tests/test_retrospective.py
  - python/tests/test_export.py
  - python/tests/test_baselines.py
autonomous: false
requirements:
  - PKG-03
  - PKG-04

must_haves:
  truths:
    - "All gsd_ml modules pass their unit tests"
    - "Test suite runs in under 30 seconds"
    - "Adapted tests verify dict config in guardrails (not Config dataclass)"
    - "Adapted tests verify subprocess git in journal and stagnation (not GitPython)"
    - "install.js validates Python package is importable"
  artifacts:
    - path: "python/tests/conftest.py"
      provides: "Shared test fixtures"
      min_lines: 20
    - path: "python/tests/test_imports.py"
      provides: "Smoke test for all module imports"
      min_lines: 15
    - path: "python/tests/test_guardrails.py"
      provides: "Guardrails tests with dict config"
      contains: "dict"
    - path: "python/tests/test_journal.py"
      provides: "Journal tests with subprocess git"
      contains: "subprocess"
  key_links:
    - from: "python/tests/test_guardrails.py"
      to: "python/src/gsd_ml/guardrails.py"
      via: "from gsd_ml.guardrails import ResourceGuardrails"
      pattern: "from gsd_ml\\.guardrails import"
    - from: "python/tests/test_imports.py"
      to: "python/src/gsd_ml/"
      via: "imports all 17 modules"
      pattern: "import gsd_ml"
---

<objective>
Port and adapt the test suite from mlforge, then run end-to-end validation of the full Phase 1 deliverable.

Purpose: Prove all ported modules work correctly with their adaptations (dict config, subprocess git). Validate the npm installer detects the Python package.
Output: Full passing test suite + validated install flow.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

Prior plan summaries (for what was built):
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

Test files to port from:
@/home/tlupo/AutoML/tests/mlforge/conftest.py
@/home/tlupo/AutoML/tests/mlforge/test_profiler.py
@/home/tlupo/AutoML/tests/mlforge/test_state.py
@/home/tlupo/AutoML/tests/mlforge/test_checkpoint.py
@/home/tlupo/AutoML/tests/mlforge/test_guardrails.py
@/home/tlupo/AutoML/tests/mlforge/test_diagnostics.py
@/home/tlupo/AutoML/tests/mlforge/test_results.py
@/home/tlupo/AutoML/tests/mlforge/test_journal.py
@/home/tlupo/AutoML/tests/mlforge/test_stagnation.py
@/home/tlupo/AutoML/tests/mlforge/test_drafts.py
@/home/tlupo/AutoML/tests/mlforge/test_retrospective.py
@/home/tlupo/AutoML/tests/mlforge/test_export.py
@/home/tlupo/AutoML/tests/mlforge/test_baselines.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port and adapt test suite from mlforge</name>
  <files>python/tests/conftest.py, python/tests/test_imports.py, python/tests/test_profiler.py, python/tests/test_state.py, python/tests/test_checkpoint.py, python/tests/test_guardrails.py, python/tests/test_diagnostics.py, python/tests/test_results.py, python/tests/test_journal.py, python/tests/test_stagnation.py, python/tests/test_drafts.py, python/tests/test_retrospective.py, python/tests/test_export.py, python/tests/test_baselines.py</files>
  <action>
Port test files from `/home/tlupo/AutoML/tests/mlforge/` to `python/tests/`. For each file:

1. Read the mlforge test file
2. Replace `from mlforge.X` imports with `from gsd_ml.X`
3. Remove any tests that depend on dropped modules (engine, cli, swarm, hooks, progress, plugins, config, git_ops, scaffold, gpu, notify, status, clean, logging_config)
4. Apply specific adaptations:

**conftest.py:**
- Port shared fixtures (tmp dirs, sample data generators)
- Remove any fixtures that reference Config dataclass or GitManager
- Add fixture for dict config (budget_experiments, budget_usd, budget_minutes, min_free_disk_gb)

**test_imports.py (NEW — not in mlforge):**
- Create a smoke test that imports every module:
  ```python
  def test_all_modules_importable():
      import gsd_ml
      from gsd_ml import state, checkpoint, profiler, guardrails
      from gsd_ml import results, journal, diagnostics, drafts, stagnation
      from gsd_ml import retrospective, export
      from gsd_ml.baselines import tabular, deeplearning, finetuning
      from gsd_ml.prepare import tabular as pt, deeplearning as pd, finetuning as pf
  ```

**test_guardrails.py — ADAPT:**
- Change all Config() instantiation to dict: `{"budget_experiments": 50, "budget_usd": 5.0, "budget_minutes": 60, "min_free_disk_gb": 1.0}`
- Change `config.budget_experiments` assertions to use dict access

**test_journal.py — ADAPT:**
- Remove GitPython-specific tests (Repo, GitCommandError)
- If tests mock GitPython, change to mock subprocess.run
- Test get_last_diff with mocked subprocess

**test_stagnation.py — ADAPT:**
- Remove GitManager references
- If tests mock GitManager, change to mock subprocess.run
- Test trigger_stagnation_branch with mocked subprocess

**test_retrospective.py — ADAPT:**
- Change Config references to dict
- Update assertions for dict-based config access

**test_export.py — ADAPT:**
- Change Config references to dict

**test_baselines.py:**
- Port tests for tabular, DL, and FT baselines
- May need to check if mlforge has separate test_dl_baselines.py and test_ft_baselines.py — if so, consolidate into test_baselines.py

After porting all tests, run the full suite:
```bash
cd /home/tlupo/gsd-ml/python && python -m pytest tests/ -v
```

Fix any failures. Common issues:
- Import paths not updated (grep for "mlforge")
- Config attribute access vs dict key access
- Missing subprocess mocks
- Test fixtures referencing dropped modules
  </action>
  <verify>
    <automated>cd /home/tlupo/gsd-ml/python && python -m pytest tests/ -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>Full test suite passes. All tests use gsd_ml imports (no mlforge references). Guardrails tests use dict config. Journal/stagnation tests use subprocess mocks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 1 deliverable</name>
  <files>N/A</files>
  <action>
Present the complete Phase 1 deliverable for human verification. All automation is complete — this checkpoint confirms the end-to-end result.

What was built:
- Python gsd_ml package with 17 ported modules and passing test suite
- npm package skeleton with install.js that copies skills/workflows to ~/.claude/
- 3 static train templates (de-Jinja2'd from mlforge)
  </action>
  <verify>
Human verifies the following:
1. Run full Python test suite: `cd /home/tlupo/gsd-ml/python && python -m pytest tests/ -v` — all tests pass
2. Verify Python package imports: `python -c "from gsd_ml.profiler import profile_dataset; from gsd_ml.guardrails import ResourceGuardrails; print('Python OK')"` — prints "Python OK"
3. Run npm installer: `cd /home/tlupo/gsd-ml && node bin/install.js` — files copied to ~/.claude/, Python validation message shown
4. Verify skill installed: `ls ~/.claude/commands/gsd-ml/ml.md` — file exists
5. Verify no mlforge references remain: `grep -r "from mlforge" python/src/ gsd-ml/` — no output
6. Verify no GitPython references: `grep -r "from git import" python/src/` — no output
  </verify>
  <done>Human approves Phase 1 deliverable. All success criteria met.</done>
</task>

</tasks>

<verification>
- `cd python && python -m pytest tests/ -v` — all pass
- `python -c "from gsd_ml.profiler import profile_dataset"` — works
- `python -c "from gsd_ml.guardrails import ResourceGuardrails"` — works
- `node bin/install.js` — copies files, validates Python
- `grep -r "from mlforge" python/src/` — no results
- `grep -r "from git import" python/src/` — no results
</verification>

<success_criteria>
- Full Python test suite passes (all 14 test files)
- All 17 gsd_ml modules import without error
- npm install.js copies skills and validates Python package
- No references to mlforge or GitPython in source
- Human approves the Phase 1 deliverable
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
