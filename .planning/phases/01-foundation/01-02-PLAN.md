---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bin/install.js
  - commands/gsd-ml/ml.md
  - gsd-ml/workflows/ml-run.md
  - gsd-ml/references/domains.md
  - gsd-ml/templates/.gitkeep
autonomous: true
requirements:
  - PKG-01
  - PKG-02

must_haves:
  truths:
    - "npm package has valid package.json with bin entry pointing to install.js"
    - "install.js copies commands/gsd-ml/ to ~/.claude/commands/gsd-ml/"
    - "install.js copies gsd-ml/ to ~/.claude/gsd-ml/"
    - "install.js does path replacement in .md files"
    - "Stub skill file exists at commands/gsd-ml/ml.md with correct frontmatter"
  artifacts:
    - path: "package.json"
      provides: "npm package configuration"
      contains: "gsd-ml"
    - path: "bin/install.js"
      provides: "Installer that copies files to ~/.claude/"
      min_lines: 50
    - path: "commands/gsd-ml/ml.md"
      provides: "Stub /gsd:ml skill file"
      contains: "name: gsd:ml"
    - path: "gsd-ml/workflows/ml-run.md"
      provides: "Stub workflow file"
    - path: "gsd-ml/references/domains.md"
      provides: "Stub domain reference"
  key_links:
    - from: "package.json"
      to: "bin/install.js"
      via: "bin.gsd-ml entry"
      pattern: "\"gsd-ml\".*install\\.js"
    - from: "bin/install.js"
      to: "commands/gsd-ml/"
      via: "copyWithPathReplacement to ~/.claude/commands/gsd-ml/"
      pattern: "commands.*gsd-ml"
    - from: "bin/install.js"
      to: "gsd-ml/"
      via: "copyWithPathReplacement to ~/.claude/gsd-ml/"
      pattern: "gsd-ml"
---

<objective>
Create the npm package skeleton that installs gsd-ml skills, workflows, references, and templates into ~/.claude/.

Purpose: Enable `npm install -g gsd-ml` (or `npx gsd-ml`) to set up Claude Code with the /gsd:ml skill. This follows the exact same pattern as get-shit-done-cc.
Output: Working npm package with install.js, stub skill file, and stub supporting files.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

GSD install.js reference (study this pattern):
@/home/tlupo/.npm/_npx/4db0de1f85c3165e/node_modules/get-shit-done-cc/bin/install.js
@/home/tlupo/.npm/_npx/4db0de1f85c3165e/node_modules/get-shit-done-cc/package.json

GSD skill file examples:
@/home/tlupo/.claude/commands/gsd/plan-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json and install.js</name>
  <files>package.json, bin/install.js</files>
  <action>
1. Create `package.json` at repo root using the exact config from 01-RESEARCH.md Code Examples section:
   - name: "gsd-ml"
   - version: "0.1.0"
   - description: "Claude Code native ML research tool - skills, workflows, and templates"
   - bin: { "gsd-ml": "bin/install.js" }
   - files: ["bin", "commands", "gsd-ml"]
   - Note: Do NOT include "python" in files — Python package is separate pip install
   - keywords, author, license, engines per RESEARCH.md

2. Create `bin/install.js` following the GSD install.js pattern exactly. Read the GSD source at `/home/tlupo/.npm/_npx/4db0de1f85c3165e/node_modules/get-shit-done-cc/bin/install.js` and adapt:
   - Use `#!/usr/bin/env node` shebang
   - Use only Node.js stdlib: fs, path, os
   - Implement `copyWithPathReplacement(srcDir, destDir, pathPrefix, dirName, isCommands)` — same as GSD's version
   - Copy `commands/gsd-ml/` to `~/.claude/commands/gsd-ml/` (skill files)
   - Copy `gsd-ml/` to `~/.claude/gsd-ml/` (workflows, references, templates)
   - Do path replacement: replace template path references in .md files with actual ~/.claude/ path
   - Add Python validation step: after copying, run `python3 -c "import gsd_ml"` (or `python -c "import gsd_ml"`) and:
     - If succeeds: print success message
     - If fails: print warning with install instructions: "pip install gsd-ml" or "pip install -e python/" for development
   - Print summary of what was installed (file counts, paths)
   - Handle --claude flag if GSD uses it (check source)
  </action>
  <verify>
    <automated>cd /home/tlupo/gsd-ml && node -c "require('./bin/install.js')" 2>&1 | head -5; test -f package.json && test -f bin/install.js && echo "FILES_EXIST"</automated>
  </verify>
  <done>package.json has correct bin entry. install.js runs without syntax errors. Both files exist at expected paths.</done>
</task>

<task type="auto">
  <name>Task 2: Create stub skill and supporting files</name>
  <files>commands/gsd-ml/ml.md, gsd-ml/workflows/ml-run.md, gsd-ml/references/domains.md, gsd-ml/templates/.gitkeep</files>
  <action>
1. Create `commands/gsd-ml/ml.md` — the /gsd:ml skill file stub. Use the exact frontmatter format from RESEARCH.md Pattern 2:
   ```markdown
   ---
   name: gsd:ml
   description: Run an autonomous ML experiment
   argument-hint: "<dataset> <target> [--domain tabular|dl|ft]"
   allowed-tools:
     - Read
     - Write
     - Edit
     - Bash
     - Glob
     - Grep
     - Task
   ---
   <objective>
   Run an autonomous ML experiment on the given dataset.
   </objective>

   <execution_context>
   @~/.claude/gsd-ml/workflows/ml-run.md
   </execution_context>

   <context>
   Dataset: $ARGUMENTS
   </context>

   <process>
   Execute the ml-run workflow end-to-end.
   </process>
   ```

2. Create `gsd-ml/workflows/ml-run.md` — stub workflow (content comes in Phase 2):
   ```markdown
   # ML Run Workflow

   > Stub — full workflow implemented in Phase 2 (Core Workflow).

   This workflow orchestrates an end-to-end ML experiment:
   1. Profile dataset
   2. Scaffold experiment directory
   3. Run experiment loop (edit → train → evaluate → keep/revert)
   4. Export best model
   5. Generate retrospective
   ```

3. Create `gsd-ml/references/domains.md` — stub domain reference:
   ```markdown
   # Domain Reference

   > Stub — full content implemented in Phase 2.

   ## Domains
   - **tabular** — sklearn, XGBoost, LightGBM on CSV/Parquet data
   - **dl** — PyTorch + timm/transformers for image/text classification
   - **ft** — LoRA/QLoRA fine-tuning via peft and trl
   ```

4. Create `gsd-ml/templates/.gitkeep` — templates directory placeholder. Actual train template files are created in Plan 03.

5. Run the installer to verify it works: `cd /home/tlupo/gsd-ml && node bin/install.js`
   - Verify files appear at `~/.claude/commands/gsd-ml/ml.md`
   - Verify files appear at `~/.claude/gsd-ml/workflows/ml-run.md`
  </action>
  <verify>
    <automated>cd /home/tlupo/gsd-ml && node bin/install.js 2>&1; test -f ~/.claude/commands/gsd-ml/ml.md && test -f ~/.claude/gsd-ml/workflows/ml-run.md && echo "INSTALL_VERIFIED"</automated>
  </verify>
  <done>Stub skill file installed at ~/.claude/commands/gsd-ml/ml.md. Workflow and reference stubs installed at ~/.claude/gsd-ml/. /gsd:ml will appear as an available skill in Claude Code.</done>
</task>

</tasks>

<verification>
- `node bin/install.js` completes without error
- `ls ~/.claude/commands/gsd-ml/ml.md` exists
- `ls ~/.claude/gsd-ml/workflows/ml-run.md` exists
- `ls ~/.claude/gsd-ml/references/domains.md` exists
- `grep "gsd:ml" commands/gsd-ml/ml.md` finds the skill name
- package.json `files` array includes bin, commands, gsd-ml but NOT python
</verification>

<success_criteria>
- npm package structure is valid (package.json + bin/install.js)
- install.js copies skills to ~/.claude/commands/gsd-ml/
- install.js copies workflows/references/templates to ~/.claude/gsd-ml/
- install.js checks for Python package and warns if missing
- /gsd:ml skill file has correct YAML frontmatter
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
