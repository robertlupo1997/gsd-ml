---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - python/src/gsd_ml/baselines/tabular.py
  - python/src/gsd_ml/baselines/deeplearning.py
  - python/src/gsd_ml/baselines/finetuning.py
  - python/src/gsd_ml/prepare/tabular.py
  - python/src/gsd_ml/prepare/deeplearning.py
  - python/src/gsd_ml/prepare/finetuning.py
  - gsd-ml/templates/train-tabular.py
  - gsd-ml/templates/train-dl.py
  - gsd-ml/templates/train-ft.py
autonomous: true
requirements:
  - PKG-03

must_haves:
  truths:
    - "All 6 subpackage modules import without error"
    - "baselines/tabular.py computes naive baselines for classification and regression"
    - "prepare/tabular.py loads and preprocesses CSV data"
    - "Train templates are static Python files with placeholder constants (not Jinja2)"
  artifacts:
    - path: "python/src/gsd_ml/baselines/tabular.py"
      provides: "Tabular baseline computation"
      min_lines: 50
    - path: "python/src/gsd_ml/prepare/tabular.py"
      provides: "Tabular data preparation"
      min_lines: 100
    - path: "gsd-ml/templates/train-tabular.py"
      provides: "Static tabular training template"
      contains: "__DATASET_PATH__"
    - path: "gsd-ml/templates/train-dl.py"
      provides: "Static DL training template"
      contains: "__DATASET_PATH__"
    - path: "gsd-ml/templates/train-ft.py"
      provides: "Static FT training template"
      contains: "__DATASET_PATH__"
  key_links:
    - from: "python/src/gsd_ml/profiler.py"
      to: "python/src/gsd_ml/prepare/tabular.py"
      via: "from gsd_ml.prepare.tabular import validate_no_leakage"
      pattern: "from gsd_ml\\.prepare\\.tabular import"
---

<objective>
Port the 6 subpackage modules (baselines/*, prepare/*) and convert 3 Jinja2 templates to static Python files.

Purpose: Complete the Python package with all domain-specific utilities and provide the train.py templates that Claude Code will use as starting points for experiments.
Output: 6 subpackage modules importable, 3 static train templates with placeholder constants.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

Source files to port from:
@/home/tlupo/AutoML/src/mlforge/tabular/baselines.py
@/home/tlupo/AutoML/src/mlforge/deeplearning/baselines.py
@/home/tlupo/AutoML/src/mlforge/finetuning/baselines.py
@/home/tlupo/AutoML/src/mlforge/tabular/prepare.py
@/home/tlupo/AutoML/src/mlforge/deeplearning/prepare.py
@/home/tlupo/AutoML/src/mlforge/finetuning/prepare.py

Templates to de-Jinja2:
@/home/tlupo/AutoML/src/mlforge/templates/tabular_train.py.j2
@/home/tlupo/AutoML/src/mlforge/templates/dl_train.py.j2
@/home/tlupo/AutoML/src/mlforge/templates/ft_train.py.j2
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port baselines and prepare subpackages</name>
  <files>python/src/gsd_ml/baselines/tabular.py, python/src/gsd_ml/baselines/deeplearning.py, python/src/gsd_ml/baselines/finetuning.py, python/src/gsd_ml/prepare/tabular.py, python/src/gsd_ml/prepare/deeplearning.py, python/src/gsd_ml/prepare/finetuning.py</files>
  <action>
Port 6 subpackage files from mlforge. These are mostly direct copies with import changes:

**baselines/ (3 files):**
- `baselines/tabular.py` (76 lines) from `/home/tlupo/AutoML/src/mlforge/tabular/baselines.py` — change any `from mlforge.X` to `from gsd_ml.X`. Likely no changes needed (pure numpy/sklearn).
- `baselines/deeplearning.py` (64 lines) from `/home/tlupo/AutoML/src/mlforge/deeplearning/baselines.py` — same treatment.
- `baselines/finetuning.py` (39 lines) from `/home/tlupo/AutoML/src/mlforge/finetuning/baselines.py` — same treatment.

**prepare/ (3 files):**
- `prepare/tabular.py` (218 lines) from `/home/tlupo/AutoML/src/mlforge/tabular/prepare.py` — change any `from mlforge.X` to `from gsd_ml.X`. This file contains `validate_no_leakage` which profiler.py imports.
- `prepare/deeplearning.py` (197 lines) from `/home/tlupo/AutoML/src/mlforge/deeplearning/prepare.py` — same treatment.
- `prepare/finetuning.py` (171 lines) from `/home/tlupo/AutoML/src/mlforge/finetuning/prepare.py` — same treatment.

After porting, verify all 6 import:
```python
python -c "
from gsd_ml.baselines.tabular import compute_baselines
from gsd_ml.baselines.deeplearning import compute_baselines as dl_baselines
from gsd_ml.baselines.finetuning import compute_baselines as ft_baselines
from gsd_ml.prepare.tabular import load_data
from gsd_ml.prepare.deeplearning import prepare_dl_data
from gsd_ml.prepare.finetuning import prepare_ft_data
print('All 6 subpackage modules imported')
"
```

Note: The exact export names may differ from what's shown above — check the actual source files for the correct function names. Adjust the verification import accordingly.
  </action>
  <verify>
    <automated>cd /home/tlupo/gsd-ml && python -c "from gsd_ml.baselines import tabular, deeplearning, finetuning; from gsd_ml.prepare import tabular as pt, deeplearning as pd, finetuning as pf; print('OK')"</automated>
  </verify>
  <done>All 6 subpackage modules (3 baselines + 3 prepare) import successfully. No mlforge imports remain. profiler.py can now successfully import validate_no_leakage from gsd_ml.prepare.tabular.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Jinja2 templates to static Python files</name>
  <files>gsd-ml/templates/train-tabular.py, gsd-ml/templates/train-dl.py, gsd-ml/templates/train-ft.py</files>
  <action>
Convert 3 Jinja2 templates from mlforge into static Python files with placeholder constants. These are NOT Python package files — they live in the npm package's gsd-ml/templates/ directory and are what Claude Code copies into .ml/ as a starting train.py.

For each template:
1. Read the .j2 source file
2. Replace all `{{ variable }}` Jinja2 expressions with Python constants at the top of the file
3. Remove all `{% if/for/block %}` Jinja2 control flow — expand to the most common case
4. Add a clear header comment explaining the placeholder constants

**Placeholder constant pattern (from RESEARCH.md Pitfall 2):**
```python
# === Configuration (filled by Claude Code) ===
CSV_PATH = "__DATASET_PATH__"
TARGET_COLUMN = "__TARGET__"
METRIC = "__METRIC__"
TASK = "__TASK__"  # "classification" or "regression"
```

**train-tabular.py** from `tabular_train.py.j2` (138 lines):
- Replace `{{ csv_path }}` with `CSV_PATH = "__DATASET_PATH__"`
- Replace `{{ target }}` with `TARGET_COLUMN = "__TARGET__"`
- Replace `{{ metric }}` with `METRIC = "__METRIC__"`
- Replace `{{ task }}` with `TASK = "__TASK__"`
- Keep the sklearn/xgboost/lightgbm training logic intact
- Keep cross_val_score evaluation
- Keep JSON metric output to stdout: `print(json.dumps({"metric_name": metric_name, "value": score}))`

**train-dl.py** from `dl_train.py.j2` (260 lines):
- Same placeholder pattern for dataset path, target, metric
- Add DL-specific placeholders: `DEVICE = "__DEVICE__"`, `NUM_EPOCHS = __NUM_EPOCHS__`, `BATCH_SIZE = __BATCH_SIZE__`
- Keep PyTorch training logic

**train-ft.py** from `ft_train.py.j2` (230 lines):
- Same placeholder pattern
- Add FT-specific placeholders: `MODEL_NAME = "__MODEL_NAME__"`, `LORA_R = __LORA_R__`, `LORA_ALPHA = __LORA_ALPHA__`
- Keep peft/trl training logic

Each file must be valid Python (no Jinja2 syntax remaining). The placeholder strings (`__DATASET_PATH__` etc.) will be replaced by Claude Code using the Edit tool during experiment setup.
  </action>
  <verify>
    <automated>cd /home/tlupo/gsd-ml && python -c "import ast; [ast.parse(open(f'gsd-ml/templates/{t}').read()) for t in ['train-tabular.py', 'train-dl.py', 'train-ft.py']]; print('All templates are valid Python')"</automated>
  </verify>
  <done>3 static train templates exist in gsd-ml/templates/. All are valid Python syntax. Jinja2 variables replaced with placeholder constants. No Jinja2 syntax ({{ }}, {% %}) remains.</done>
</task>

</tasks>

<verification>
- All 6 subpackage modules import successfully
- `grep -r "from mlforge" python/src/` returns no results
- All 3 template files parse as valid Python
- `grep -r "{{" gsd-ml/templates/` returns no results (no Jinja2 remaining)
- `grep "__DATASET_PATH__" gsd-ml/templates/train-tabular.py` finds the placeholder
- profiler.py can now import from prepare.tabular: `python -c "from gsd_ml.profiler import profile_dataset"`
</verification>

<success_criteria>
- All 17 Python modules now importable (11 root + 6 subpackage)
- 3 static train templates with placeholder constants
- Zero Jinja2 syntax remaining in templates
- profiler.py import chain complete (depends on prepare.tabular)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
