---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/pyproject.toml
  - python/src/gsd_ml/__init__.py
  - python/src/gsd_ml/baselines/__init__.py
  - python/src/gsd_ml/prepare/__init__.py
  - python/src/gsd_ml/state.py
  - python/src/gsd_ml/checkpoint.py
  - python/src/gsd_ml/profiler.py
  - python/src/gsd_ml/guardrails.py
  - python/src/gsd_ml/results.py
  - python/src/gsd_ml/journal.py
  - python/src/gsd_ml/diagnostics.py
  - python/src/gsd_ml/drafts.py
  - python/src/gsd_ml/stagnation.py
  - python/src/gsd_ml/retrospective.py
  - python/src/gsd_ml/export.py
autonomous: true
requirements:
  - PKG-03

must_haves:
  truths:
    - "gsd_ml package is pip-installable from python/ directory"
    - "All 11 root-level modules import without error"
    - "Modules that depend on state.py can import SessionState"
    - "guardrails.py accepts dict config instead of Config dataclass"
    - "journal.py uses subprocess git instead of GitPython"
    - "stagnation.py uses subprocess git instead of GitPython"
  artifacts:
    - path: "python/pyproject.toml"
      provides: "Python package build configuration"
      contains: "gsd-ml"
    - path: "python/src/gsd_ml/__init__.py"
      provides: "Package root with version"
      contains: "__version__"
    - path: "python/src/gsd_ml/state.py"
      provides: "SessionState dataclass"
      exports: ["SessionState"]
    - path: "python/src/gsd_ml/guardrails.py"
      provides: "Resource guardrails with dict config"
      exports: ["ResourceGuardrails"]
    - path: "python/src/gsd_ml/journal.py"
      provides: "Experiment journal without GitPython"
      exports: ["ExperimentJournal"]
    - path: "python/src/gsd_ml/stagnation.py"
      provides: "Stagnation detection without GitPython"
      exports: ["check_stagnation", "trigger_stagnation_branch"]
  key_links:
    - from: "python/src/gsd_ml/checkpoint.py"
      to: "python/src/gsd_ml/state.py"
      via: "from gsd_ml.state import SessionState"
      pattern: "from gsd_ml\\.state import"
    - from: "python/src/gsd_ml/guardrails.py"
      to: "dict config"
      via: "config['budget_experiments'] instead of config.budget_experiments"
      pattern: "config\\[\"budget_|config\\.get\\("
    - from: "python/src/gsd_ml/journal.py"
      to: "subprocess"
      via: "subprocess.run(['git', 'diff'])"
      pattern: "subprocess\\.run.*git"
---

<objective>
Create the Python package scaffold and port all 11 root-level gsd_ml modules from mlforge.

Purpose: Establish the gsd_ml Python package with all core utilities ported and adapted from mlforge. This is the foundation for all ML functionality.
Output: Pip-installable python/ directory with 11 working modules.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

Source files to port from:
@/home/tlupo/AutoML/src/mlforge/state.py
@/home/tlupo/AutoML/src/mlforge/checkpoint.py
@/home/tlupo/AutoML/src/mlforge/profiler.py
@/home/tlupo/AutoML/src/mlforge/guardrails.py
@/home/tlupo/AutoML/src/mlforge/results.py
@/home/tlupo/AutoML/src/mlforge/journal.py
@/home/tlupo/AutoML/src/mlforge/intelligence/diagnostics.py
@/home/tlupo/AutoML/src/mlforge/intelligence/drafts.py
@/home/tlupo/AutoML/src/mlforge/intelligence/stagnation.py
@/home/tlupo/AutoML/src/mlforge/retrospective.py
@/home/tlupo/AutoML/src/mlforge/export.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Python package scaffold</name>
  <files>python/pyproject.toml, python/src/gsd_ml/__init__.py, python/src/gsd_ml/baselines/__init__.py, python/src/gsd_ml/prepare/__init__.py</files>
  <action>
Create the python/ directory structure for the gsd_ml package:

1. Create `python/pyproject.toml` using the exact config from 01-RESEARCH.md Code Examples section. Key points:
   - name: "gsd-ml", version: "0.1.0"
   - requires-python: ">=3.11"
   - dependencies: scikit-learn>=1.5, pandas>=2.0, numpy>=2.0, xgboost>=2.0, lightgbm>=4.0, pyarrow>=15.0
   - optional-dependencies for dl and ft (Phase 4, include now for completeness)
   - dependency-groups: dev = ["pytest", "ruff"]
   - build-system: hatchling
   - tool.hatch.build.targets.wheel: packages = ["src/gsd_ml"]
   - ruff config: target-version = "py311", line-length = 100
   - pytest: testpaths = ["tests"]

2. Create `python/src/gsd_ml/__init__.py` with only:
   ```python
   """gsd_ml — Python ML utilities for gsd-ml."""
   __version__ = "0.1.0"
   ```
   Keep it minimal to avoid circular imports (per Pitfall 1 in RESEARCH.md).

3. Create `python/src/gsd_ml/baselines/__init__.py` — empty file.

4. Create `python/src/gsd_ml/prepare/__init__.py` — empty file.

5. Create `python/tests/__init__.py` — empty file.

6. Install the package in dev mode: `cd /home/tlupo/gsd-ml && pip install -e "python/[dev]"` (or use uv if available).

7. Verify: `python -c "import gsd_ml; print(gsd_ml.__version__)"` prints "0.1.0".
  </action>
  <verify>
    <automated>cd /home/tlupo/gsd-ml && python -c "import gsd_ml; print(gsd_ml.__version__)" | grep -q "0.1.0"</automated>
  </verify>
  <done>python/ directory exists with pyproject.toml, all __init__.py files, package installs and imports successfully</done>
</task>

<task type="auto">
  <name>Task 2: Port all 11 root-level modules from mlforge</name>
  <files>python/src/gsd_ml/state.py, python/src/gsd_ml/checkpoint.py, python/src/gsd_ml/profiler.py, python/src/gsd_ml/guardrails.py, python/src/gsd_ml/results.py, python/src/gsd_ml/journal.py, python/src/gsd_ml/diagnostics.py, python/src/gsd_ml/drafts.py, python/src/gsd_ml/stagnation.py, python/src/gsd_ml/retrospective.py, python/src/gsd_ml/export.py</files>
  <action>
Port each mlforge module to gsd_ml. Read each source file, apply adaptations, write to destination.

**Import-only changes (8 files)** — replace `from mlforge.X` with `from gsd_ml.X`:
- `state.py` (28 lines) — copy from `/home/tlupo/AutoML/src/mlforge/state.py`, change any mlforge imports to gsd_ml
- `checkpoint.py` (72 lines) — change `from mlforge.state import SessionState` to `from gsd_ml.state import SessionState`
- `profiler.py` (171 lines) — change `from mlforge.tabular.prepare import validate_no_leakage` to `from gsd_ml.prepare.tabular import validate_no_leakage`
- `results.py` (136 lines) — change mlforge imports to gsd_ml
- `diagnostics.py` (157 lines) — copy from `/home/tlupo/AutoML/src/mlforge/intelligence/diagnostics.py`, pure numpy, likely no import changes needed
- `drafts.py` (131 lines) — copy from `/home/tlupo/AutoML/src/mlforge/intelligence/drafts.py`, pure data, likely no import changes needed
- `retrospective.py` (88 lines) — change `from mlforge.config import Config` to accept dict params, change `from mlforge.results import ResultsTracker` to `from gsd_ml.results import ResultsTracker`, change `from mlforge.state import SessionState` to `from gsd_ml.state import SessionState`
- `export.py` (84 lines) — change `from mlforge.config import Config` to accept dict params, change `from mlforge.state import SessionState` to `from gsd_ml.state import SessionState`

**Substantive adaptations (3 files):**

- `guardrails.py` (157 lines) — Read from `/home/tlupo/AutoML/src/mlforge/guardrails.py`:
  1. Remove `from mlforge.config import Config`
  2. Change `from mlforge.state import SessionState` to `from gsd_ml.state import SessionState`
  3. Change constructor signature: `config: Config` to `config: dict`
  4. Replace ALL `self.config.X` attribute access with `self.config["X"]` or `self.config.get("X", default)`
  5. See RESEARCH.md guardrails.py example for exact pattern

- `journal.py` (142 lines) — Read from `/home/tlupo/AutoML/src/mlforge/journal.py`:
  1. Remove `from git import GitCommandError, Repo` (GitPython)
  2. Add `import subprocess`
  3. Replace `get_last_diff()` implementation with subprocess.run(["git", "diff", "HEAD~1"]) pattern from RESEARCH.md
  4. Change any `from mlforge.X` to `from gsd_ml.X`

- `stagnation.py` (55 lines) — Read from `/home/tlupo/AutoML/src/mlforge/intelligence/stagnation.py`:
  1. Remove `from mlforge.git_ops import GitManager`
  2. Add `import subprocess`
  3. Change `from mlforge.state import SessionState` to `from gsd_ml.state import SessionState`
  4. Replace GitManager/Repo calls with subprocess.run(["git", ...]) pattern from RESEARCH.md

For `retrospective.py` and `export.py`, wherever `Config` was used as a type hint or parameter, change to `dict` and update attribute access to dict key access.

After porting all 11 files, verify they all import:
```python
python -c "
from gsd_ml.state import SessionState
from gsd_ml.checkpoint import save_checkpoint, load_checkpoint
from gsd_ml.profiler import profile_dataset
from gsd_ml.guardrails import ResourceGuardrails
from gsd_ml.results import ResultsTracker
from gsd_ml.journal import ExperimentJournal
from gsd_ml.diagnostics import run_diagnostics
from gsd_ml.drafts import ALGORITHM_FAMILIES
from gsd_ml.stagnation import check_stagnation
from gsd_ml.retrospective import generate_retrospective
from gsd_ml.export import export_best_model
print('All 11 modules imported successfully')
"
```

Note: profiler.py imports from gsd_ml.prepare.tabular which doesn't exist yet (Plan 03). That import will fail at call time but not at import time if it's inside a function. If it's at module level, wrap it in a conditional or move it to function-level import to avoid breaking the import chain. Check the source — if `validate_no_leakage` is imported at top level, move the import inside the function that uses it.
  </action>
  <verify>
    <automated>cd /home/tlupo/gsd-ml && python -c "from gsd_ml.state import SessionState; from gsd_ml.checkpoint import save_checkpoint, load_checkpoint; from gsd_ml.guardrails import ResourceGuardrails; from gsd_ml.results import ResultsTracker; from gsd_ml.journal import ExperimentJournal; from gsd_ml.diagnostics import run_diagnostics; from gsd_ml.drafts import ALGORITHM_FAMILIES; from gsd_ml.stagnation import check_stagnation; from gsd_ml.retrospective import generate_retrospective; from gsd_ml.export import export_best_model; print('OK')"</automated>
  </verify>
  <done>All 11 root-level gsd_ml modules import successfully. guardrails.py uses dict config. journal.py and stagnation.py use subprocess git instead of GitPython. No mlforge imports remain.</done>
</task>

</tasks>

<verification>
- `pip install -e "python/[dev]"` succeeds
- `python -c "import gsd_ml"` works
- All 11 modules import without error
- `grep -r "from mlforge" python/src/` returns no results (no mlforge imports remain)
- `grep -r "from git import" python/src/` returns no results (no GitPython imports remain)
</verification>

<success_criteria>
- gsd_ml package installs from python/ directory
- All 11 root-level modules import without error
- guardrails.py constructor accepts dict, not Config dataclass
- journal.py get_last_diff uses subprocess, not GitPython
- stagnation.py trigger_stagnation_branch uses subprocess, not GitPython
- No references to mlforge or GitPython remain in source
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
