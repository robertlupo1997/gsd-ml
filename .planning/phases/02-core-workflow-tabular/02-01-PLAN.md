---
phase: 02-core-workflow-tabular
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gsd-ml/templates/train-tabular.py
  - gsd-ml/references/metric-map.md
  - commands/gsd-ml/ml.md
autonomous: true
requirements: [SCAF-03, TAB-01, TAB-02, TAB-03, PROF-02]

must_haves:
  truths:
    - "Static train-tabular.py template is valid Python that runs without syntax errors"
    - "Template includes sklearn, XGBoost, and LightGBM with try/except imports"
    - "Template uses cross-validation via prepare.evaluate() not manual train/test split"
    - "Template outputs JSON to stdout with metric_value and metric_name keys"
    - "Metric map covers all 6 tabular metrics (accuracy, f1, f1_weighted, r2, rmse, mae)"
    - "Skill file references ml-run.md workflow correctly"
  artifacts:
    - path: "gsd-ml/templates/train-tabular.py"
      provides: "Static starter train.py for tabular ML experiments"
      min_lines: 60
      contains: "RandomForestClassifier"
    - path: "gsd-ml/references/metric-map.md"
      provides: "Metric name to sklearn scoring string mapping"
      contains: "accuracy"
    - path: "commands/gsd-ml/ml.md"
      provides: "Updated skill entry point with proper argument parsing"
      contains: "ml-run.md"
  key_links:
    - from: "gsd-ml/templates/train-tabular.py"
      to: "prepare.py (gsd_ml.prepare.tabular)"
      via: "from prepare import load_data, split_data, build_preprocessor, evaluate"
      pattern: "from prepare import"
    - from: "gsd-ml/templates/train-tabular.py"
      to: "stdout JSON contract"
      via: "print(json.dumps({metric_value, metric_name}))"
      pattern: "json\\.dumps.*metric_value"
---

<objective>
Create the static template and reference files that the ml-run workflow will use during experiment scaffolding.

Purpose: The workflow needs a starter train.py template (de-Jinja2'd from mlforge's tabular_train.py.j2) and a metric reference. These are static artifacts that get installed to ~/.claude/gsd-ml/ and referenced by the workflow.

Output: train-tabular.py template, metric-map.md reference, updated skill file
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-workflow-tabular/02-RESEARCH.md

Key patterns from Phase 1:
- install.js already copies entire gsd-ml/ directory recursively -- adding templates/ and references/ subdirs automatically picked up
- Path replacement in .md files: ~/.claude/ replaced with actual install path
- Python utilities use dict config, subprocess git, lazy imports

Source template to de-Jinja2: /home/tlupo/AutoML/src/mlforge/templates/tabular_train.py.j2
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static train-tabular.py template and metric-map.md reference</name>
  <files>gsd-ml/templates/train-tabular.py, gsd-ml/references/metric-map.md</files>
  <action>
**Create `gsd-ml/templates/train-tabular.py`:**

De-Jinja2 the mlforge template at `/home/tlupo/AutoML/src/mlforge/templates/tabular_train.py.j2`. Convert all Jinja2 constructs to Python string placeholders.

Key requirements:
- Replace `{{ csv_path }}` with `__CSV_PATH__`, `{{ target_column }}` with `__TARGET_COLUMN__`, `{{ metric }}` with `__METRIC__`, `{{ task }}` with `__TASK__`
- Remove Optuna dependency entirely -- Claude Code iterates manually, no HPO framework needed. Replace the Optuna objective function with a simple `main()` function
- Include BOTH classification and regression imports (RandomForestClassifier + RandomForestRegressor, LogisticRegression + Ridge) -- Claude Code uncomments/edits the appropriate ones
- Keep try/except imports for xgboost and lightgbm (TAB-01)
- Use `from prepare import load_data, split_data, build_preprocessor, evaluate` (frozen data pipeline)
- Call `evaluate(model, X_train_proc, y_train, scoring=METRIC, task=TASK)` for cross-validation (TAB-03)
- After CV evaluation, retrain on full train set, save model as `best_model.joblib`, save predictions as `predictions.csv`
- Print JSON to stdout as LAST line: `print(json.dumps({"metric_value": result["mean"], "metric_name": METRIC}))`
- Remove Jinja2 conditionals -- make it a single unified template that works for both classification and regression (task type is a string constant)
- Remove `{{ date_column }}` temporal note -- not needed for v1
- Remove `{{ time_budget }}` -- guardrails are in the workflow, not train.py
- Add docstring explaining this is the ONLY file Claude may modify, and prepare.py is frozen

Follow the pattern shown in the research doc's "Static train-tabular.py Template" section but WITHOUT Optuna.

**Create `gsd-ml/references/metric-map.md`:**

Create a reference document mapping metric names to sklearn scoring strings and task types:

| Metric | sklearn scoring | Task | Direction |
|--------|----------------|------|-----------|
| accuracy | accuracy | classification | maximize |
| f1 | f1 | binary classification | maximize |
| f1_weighted | f1_weighted | multiclass classification | maximize |
| r2 | r2 | regression | maximize |
| rmse | neg_root_mean_squared_error | regression | minimize |
| mae | neg_mean_absolute_error | regression | minimize |

Include a note that `profiler.profile_dataset()` auto-selects the metric, so this reference is for Claude Code's understanding when interpreting results (e.g., knowing rmse is "lower is better").
  </action>
  <verify>
    <automated>cd /home/tlupo/gsd-ml && python -c "compile(open('gsd-ml/templates/train-tabular.py').read(), 'train-tabular.py', 'exec'); print('Syntax OK')" && test -f gsd-ml/references/metric-map.md && echo "All files exist"</automated>
  </verify>
  <done>
- train-tabular.py is valid Python (compiles without error)
- Contains placeholder constants (__CSV_PATH__, __TARGET_COLUMN__, __METRIC__, __TASK__)
- Imports from prepare module (load_data, split_data, build_preprocessor, evaluate)
- Has try/except for xgboost and lightgbm
- Outputs JSON with metric_value and metric_name
- metric-map.md covers all 6 metrics with sklearn scoring strings
  </done>
</task>

<task type="auto">
  <name>Task 2: Update skill file with proper argument parsing</name>
  <files>commands/gsd-ml/ml.md</files>
  <action>
Update the stub `commands/gsd-ml/ml.md` skill file. Currently it's minimal:

```
---
name: gsd:ml
description: Run an autonomous ML experiment
argument-hint: "<dataset> <target> [--domain tabular|dl|ft]"
allowed-tools: [Read, Write, Edit, Bash, Glob, Grep, Task]
---
<objective>Run an autonomous ML experiment.</objective>
<execution_context>@~/.claude/gsd-ml/workflows/ml-run.md</execution_context>
<context>Dataset: $ARGUMENTS</context>
<process>Execute the ml-run workflow end-to-end.</process>
```

Update to:
- Keep the YAML frontmatter as-is (name, description, argument-hint, allowed-tools)
- Update `<objective>` to be more specific: "Run an autonomous tabular ML experiment: profile dataset, scaffold .ml/ directory, iterate on train.py, export best model."
- Update `<context>` to parse $ARGUMENTS: "Dataset path: first argument. Target column: second argument. Domain defaults to tabular."
- Update `<process>` to clearly instruct: "Follow the ml-run workflow step by step. Do not skip phases. Do not modify prepare.py."
- Add `<references>` section pointing to metric-map: `@~/.claude/gsd-ml/references/metric-map.md`
  </action>
  <verify>
    <automated>cd /home/tlupo/gsd-ml && grep -q "ml-run.md" commands/gsd-ml/ml.md && grep -q "metric-map" commands/gsd-ml/ml.md && echo "Skill file updated"</automated>
  </verify>
  <done>
- Skill file parses dataset path and target column from arguments
- References both ml-run.md workflow and metric-map.md
- Instructs Claude Code to follow workflow step by step
  </done>
</task>

</tasks>

<verification>
- `python -c "compile(open('gsd-ml/templates/train-tabular.py').read(), 'train-tabular.py', 'exec')"` succeeds
- `gsd-ml/references/metric-map.md` exists and contains all 6 metrics
- `commands/gsd-ml/ml.md` references workflow and metric-map
- Existing tests still pass: `cd /home/tlupo/gsd-ml && python -m pytest python/tests/ -x -q`
</verification>

<success_criteria>
- Static train-tabular.py template compiles as valid Python
- Template has all 4 placeholder constants and JSON output contract
- Metric map covers accuracy, f1, f1_weighted, r2, rmse, mae
- Skill file properly wired to workflow and references
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-workflow-tabular/02-01-SUMMARY.md`
</output>
