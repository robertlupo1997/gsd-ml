---
phase: 04-dl-fine-tuning
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - gsd-ml/workflows/ml-run.md
autonomous: true
requirements: [DL-01, DL-02, DL-03, DL-04, FT-01, FT-02, FT-03, FT-04]

must_haves:
  truths:
    - "Workflow routes to DL image classification when domain=dl and task=image_classification"
    - "Workflow routes to DL text classification when domain=dl and task=text_classification"
    - "Workflow routes to FT when domain=ft"
    - "DL profiling validates directory structure for images or CSV/JSON for text instead of pandas profiler"
    - "FT skips profiling entirely -- sets task=sft, metric defaults to perplexity"
    - "DL scaffolding uses train-dl-image.py or train-dl-text.py template and deeplearning prepare module"
    - "FT scaffolding uses train-ft.py template and finetuning prepare module"
    - "DL baselines compute with labels array from data, FT baselines compute with vocab_size"
    - "GPU warning surfaced early for DL and FT domains"
    - "DL finalize exports best_model.pt, FT finalize exports best_adapter/ directory"
  artifacts:
    - path: "gsd-ml/workflows/ml-run.md"
      provides: "Multi-domain experiment workflow"
      contains: "domain"
  key_links:
    - from: "gsd-ml/workflows/ml-run.md"
      to: "gsd-ml/templates/train-dl-image.py"
      via: "template reference in scaffold step"
      pattern: "train-dl-image.py"
    - from: "gsd-ml/workflows/ml-run.md"
      to: "gsd-ml/templates/train-ft.py"
      via: "template reference in scaffold step"
      pattern: "train-ft.py"
    - from: "gsd-ml/workflows/ml-run.md"
      to: "gsd_ml.baselines.deeplearning"
      via: "Python import in baseline step"
      pattern: "gsd_ml.baselines.deeplearning"
    - from: "gsd-ml/workflows/ml-run.md"
      to: "gsd_ml.baselines.finetuning"
      via: "Python import in baseline step"
      pattern: "gsd_ml.baselines.finetuning"
---

<objective>
Add domain routing to the ml-run.md workflow so it handles DL (image and text classification) and FT (LoRA/QLoRA fine-tuning) experiments end-to-end, in addition to the existing tabular path.

Purpose: This is what makes `/gsd:ml dataset target --domain dl` and `/gsd:ml data.jsonl target --domain ft` actually work.
Output: Updated ml-run.md with domain-conditional branches at profile, scaffold, baseline, loop, and finalize phases.
</objective>

<execution_context>
@/home/tlupo/.claude/get-shit-done/workflows/execute-plan.md
@/home/tlupo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dl-fine-tuning/04-RESEARCH.md
@.planning/phases/04-dl-fine-tuning/04-01-SUMMARY.md

Workflow to extend:
@gsd-ml/workflows/ml-run.md

Templates created in Plan 01:
@gsd-ml/templates/train-dl-image.py
@gsd-ml/templates/train-dl-text.py
@gsd-ml/templates/train-ft.py

Python modules used in routing:
@python/src/gsd_ml/prepare/deeplearning.py
@python/src/gsd_ml/prepare/finetuning.py
@python/src/gsd_ml/baselines/deeplearning.py
@python/src/gsd_ml/baselines/finetuning.py
@python/src/gsd_ml/drafts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DL and FT domain routing to ml-run.md workflow</name>
  <files>gsd-ml/workflows/ml-run.md</files>
  <action>
    Extend ml-run.md with domain-conditional branches. The existing tabular path stays exactly as-is. New branches are added at each phase using the pattern: "**If domain == 'tabular':** [existing code]" / "**If domain == 'dl':** [new code]" / "**If domain == 'ft':** [new code]".

    **Phase 1 (Profile) changes:**

    Step 1.1: Add `task` to parsed arguments (for DL: "image_classification" or "text_classification"). Add `model_name` (for FT: HuggingFace model name, for DL: optional timm/HF model name).

    Step 1.2: For DL, verify the dataset path exists (directory for images, file for text). For FT, verify the data file exists (JSONL/JSON/CSV).

    Step 1.3: Add DL branch -- for image_classification: count subdirectories as classes, count images, set metric=accuracy, direction=maximize. For text_classification: read CSV/JSON, detect num_labels, set metric=f1_weighted, direction=maximize. Add FT branch -- skip profiling, set task="sft", metric defaults to "perplexity" (or user-specified), direction="minimize" (except ROUGE metrics which are "maximize").

    Add Step 1.3a (new, after Step 1.3): GPU check for DL and FT domains only:
    ```bash
    python -c "
    from gsd_ml.prepare.deeplearning import get_device_info
    import json
    info = get_device_info()
    print(json.dumps(info))
    "
    ```
    If device is "cpu", print WARNING: "No GPU detected. DL/FT training will be very slow." Do NOT block -- just warn.

    **Phase 2 (Scaffold) changes:**

    Step 2.3 (config.json): Add domain-specific fields:
    - DL: `"domain": "dl"`, `"task": "{image_classification|text_classification}"`, `"model_name": "{model}"`, `"img_size": 224` (image only), no draft_families override (use defaults from drafts.py)
    - FT: `"domain": "ft"`, `"task": "sft"`, `"model_name": "{model_name}"`, `"lora_r": 8`, `"lora_alpha": 8`, `"max_length": 512`, `"learning_rate": 2e-4`, `"num_epochs": 3`, `"budget_experiments": 10`, `"budget_minutes": 120`

    Step 2.4 (prepare.py): For DL, copy `gsd_ml.prepare.deeplearning` source. For FT, copy `gsd_ml.prepare.finetuning` source.

    Step 2.5 (train.py): For DL image, read `~/.claude/gsd-ml/templates/train-dl-image.py` and replace placeholders. For DL text, read `~/.claude/gsd-ml/templates/train-dl-text.py` and replace placeholders. For FT, read `~/.claude/gsd-ml/templates/train-ft.py` and replace placeholders.

    Step 2.7 (baselines): For DL:
    ```bash
    cd .ml && python -c "
    import json, numpy as np
    from torchvision.datasets import ImageFolder
    from gsd_ml.baselines.deeplearning import compute_baselines
    # For image: get labels from ImageFolder
    # For text: get labels from CSV/JSON
    labels = np.array(...)  # extracted from data
    baselines = compute_baselines(labels, '{metric}', '{task}')
    print(json.dumps(baselines, default=str))
    "
    ```
    For FT:
    ```bash
    cd .ml && python -c "
    import json
    from transformers import AutoTokenizer
    from gsd_ml.baselines.finetuning import compute_baselines
    tokenizer = AutoTokenizer.from_pretrained('{model_name}')
    vocab_size = tokenizer.vocab_size
    baselines = compute_baselines('{metric}', vocab_size)
    print(json.dumps(baselines, default=str))
    "
    ```

    Step 2.8 (multi-draft): For DL, use `get_families_for_domain('deeplearning')`. Draft families for image: try different model architectures (resnet, vit, efficientnet). Draft families for text: try different pretrained models. For FT, use `get_families_for_domain('finetuning')`. Draft families: try different LoRA configurations (r=8, r=16, r=32).

    **Phase 3 (Loop) changes:**

    Step 3.2 (edit train.py): Add domain-specific guidance. DL: try different architectures, learning rates, augmentation, schedulers. FT: try different LoRA ranks, learning rates, batch sizes, epochs.

    Step 3.4 (baseline gate): For DL, use `gsd_ml.baselines.deeplearning.passes_baseline_gate`. For FT, use `gsd_ml.baselines.finetuning.passes_baseline_gate`.

    Step 3.5a (diagnostics): Note that for FT with loss/perplexity metrics, predictions.csv contains per-sample losses (y_true=0.0, y_pred=loss_value) -- diagnostics may not be meaningful. Skip diagnostics for FT when metric is loss or perplexity.

    Step 3.5b (stagnation): For DL, use `get_families_for_domain('deeplearning')`. For FT, use `get_families_for_domain('finetuning')`.

    **Phase 4 (Finalize) changes:**

    Step 4.1 (export): For DL, the artifact is `best_model.pt` (state_dict). For FT, the artifact is the `best_adapter/` directory (LoRA weights + tokenizer). The export_artifact function already handles different file types, but note the difference in the summary.

    **Important Rules section:** Add rule 10: "For DL/FT domains, the `domain` field in config.json determines which prepare module, template, and baseline module to use. The workflow reads this field to route correctly."

    Keep the workflow readable. Use "**If domain == X:**" sections clearly indented under each step. Do NOT create separate workflow files -- keep everything in ml-run.md as the research recommended.
  </action>
  <verify>
    <automated>grep -c "domain" /home/tlupo/gsd-ml/gsd-ml/workflows/ml-run.md | xargs test 10 -le</automated>
  </verify>
  <done>
    - ml-run.md has domain routing for tabular, dl, and ft at all 4 phases
    - DL image classification path references train-dl-image.py template
    - DL text classification path references train-dl-text.py template
    - FT path references train-ft.py template
    - GPU warning added for DL/FT
    - DL baselines use deeplearning module, FT baselines use finetuning module
    - FT export references best_adapter/ directory
    - Existing tabular path is unchanged
  </done>
</task>

</tasks>

<verification>
1. Workflow mentions all 3 templates: `grep -E "train-dl-image|train-dl-text|train-ft" gsd-ml/workflows/ml-run.md`
2. Workflow imports from correct baseline modules: `grep -E "baselines\.(deeplearning|finetuning)" gsd-ml/workflows/ml-run.md`
3. Workflow has GPU check: `grep "get_device_info\|get_vram_info" gsd-ml/workflows/ml-run.md`
4. Full test suite still passes: `cd /home/tlupo/gsd-ml && python -m pytest python/tests/ -x -q`
</verification>

<success_criteria>
- ml-run.md workflow handles tabular, DL, and FT domains end-to-end
- Domain routing is clear and follows inline conditional pattern
- GPU warning surfaces early for DL/FT
- No changes to the tabular path behavior
</success_criteria>

<output>
After completion, create `.planning/phases/04-dl-fine-tuning/04-02-SUMMARY.md`
</output>
